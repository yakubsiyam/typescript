import methods from "./utils/index";

const add = (param1: number, param2: number, param3: number) => {
  return param1 + param2 + param3;
};

const res = methods.addTwo(2, 3);
const res1 = methods.multiply(2, 3);
const res2 = methods.subtract(2, 3);
